# SysInfo v1.5.2

## Summary

This release fixes the GitHub Actions workflow shape so CI failures provide usable job output, while preserving the v1.5.x hardening work.

## Verification

Use the checksum file at `checksums/v1.5.2.sha256` to verify release files:

```sh
sha256sum -c checksums/v1.5.2.sha256
```

## Included Checksums

- `system-info.sh`
- `install-system-info.sh`
- `README.md`
